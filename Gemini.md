# Local Media Hub 開発ガイドライン (Gemini.md)

このプロジェクトにおける実装方針、パフォーマンス最適化、およびUX設計の基本原則をまとめます。

## 1. 基本アーキテクチャ
- **Frontend**: React 19, motion (アニメーション).
- **Backend**: Express, Node.js (tsx).
- **Metadata**: `images/.metadata.json` に一括保存。

## 2. パス処理の鉄則 (Windows互換性)
Windowsのバックスラッシュ(`\`)とURLのスラッシュ(`/`)の混在による判定ミスを防ぐため、以下のルールを厳守してください。
- **正規化の徹底**: パスを比較・表示する際は必ず `normalizePath` 関数を通し、`\` を `/` に変換し、重複するスラッシュを掃除してください。
- **エンコード**: 日本語ファイル名に対応するため、APIリクエスト時は `encodeURIComponent` を使用し、サーバー側で `decodeURIComponent` してください。

## 3. パフォーマンス最適化
### 3.1 サーバーサイド (O(N) 指針)
- **メタデータ・キャッシュ**: 全てのリクエストでファイルを読み込まず、`metadataCache` 変数によるメモリキャッシュを利用してください。
- **タグ集計の効率化**: フォルダ一覧取得時、全メタデータをループしてフォルダごとに検索するのは禁止です。一度のスキャンで親階層へタグを波及させる事前集計ロジックを維持してください。

### 3.2 フロントエンド (レンダリング抑制)
- **検索の遅延反映**: 入力による重いフィルタリング処理には `useDeferredValue` を使用し、タイピングのレスポンスを優先してください。
- **楽観的更新 (Optimistic UI)**: タグの追加・削除などはサーバーのレスポンスを待って全体をリロードせず、フロントエンドのステートを直接書き換えて即時反映させてください。
- **サイレント更新**: バックグラウンドでのデータ同期には `loadPathContent(true)` を使用し、ロード画面（スピナー）を出さないようにしてください。

## 4. UI/UX 原則
- **日本語優先**: ボタン、ツールチップ、エラーメッセージはすべて直感的な日本語で記述してください。
- **ツールチップの付与**: アイコンのみのボタンには必ず `title` 属性を設定してください。
- **操作の永続性**:
    - ズーム状態はスライドショーの画像切り替えを跨いで維持すること。
    - フォルダ移動時は原則として検索条件をリセットするが、タグクリックによる遷移のみ条件を保持すること。
- **一括操作**: 複数選択時は画面下部にアクションバーを表示し、移動・削除・タグ付けを集約してください。

## 5. メタデータ (Tags) の整合性
ファイルの実体操作とメタデータは常に同期させる必要があります。
- **リネーム**: フォルダのリネーム時は、その配下にある全ファイルのパスキーを再帰的に書き換えること。
- **移動/削除**: 移動時はキーを付け替え、削除時はメタデータから該当するキーを削除すること。

## 6. セキュリティ
- **パス・トラバーサル防止**: サーバー側で `path.resolve` と `path.relative` を使い、操作対象が必ず `IMAGES_DIR` の配下にあることを検証してください。

## 7. マニュアル管理
- 新機能を追加した際は、必ず `README.md` を更新し、アプリ内のヘルプモーダルから確認できるようにしてください。

## 8. テスト戦略
- **フレームワーク**: Jest と React Testing Library を使用します。
- **UIテスト**: 新機能を追加した場合は、必ず `src/App.test.tsx` にテストケースを追加し、`npm run test` でパスすることを確認してください。
- **モック**: `lucide-react` のアイコンはテスト環境でのエラーを防ぐため、テスト内では `<span data-testid="icon-IconName">IconName</span>` のようにモック化して検証してください。

---
*このガイドラインは Gemini Code Assist との協調作業によって生成されました。*